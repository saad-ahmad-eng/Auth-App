/**
 * Audit Logger: structured, append-only recording of every security-relevant
 * event (login, logout, upload, download, lock, unlock, failures).
 * See Architecture.md §2.9, Security.md §9, Decision.md ADR-008.
 * Populated starting Implementation.md Phase 7.
 */
package com.authlock.server.audit;
